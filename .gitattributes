# Byte-reproducible generated artifacts and generation tooling must be stored
# and checked out as LF on every platform, so the CI "regeneration == committed"
# git-diff reproducibility gate (R6) holds regardless of a contributor's
# core.autocrlf setting.
generated/**    text eol=lf
tools/viz/*.py  text eol=lf
